# Домен, для которого Caddy выпустит HTTPS-сертификат.
# Скопируй этот файл в .env и впиши свой домен.
#
# Пример:
#   DOMAIN=gorbushka.example.ru
#
# Локально (без домена) можно оставить localhost — Caddy запустится без TLS.
DOMAIN=example.ru
